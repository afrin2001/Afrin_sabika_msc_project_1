# Java CI/CD Project using Jenkins

This is a simple Maven-based Java project to demonstrate a CI/CD pipeline using Jenkins.

## Pipeline Steps

1. Clone code from repository
2. Build using Maven
3. Run JUnit tests
4. Package the application

## Run locally

Compile:

mvn clean compile

Run tests:

mvn test